root = "."

tmp_dir = "tmp"

[build]
  cmd = "go build -o /tmp/william-admin-server ./cmd/admin-server"
  bin = "/tmp/william-admin-server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "gen", "node_modules"]

[log]
  time = true
